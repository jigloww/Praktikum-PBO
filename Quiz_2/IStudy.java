public interface IStudy {
    void belajar();
}

// abstract class = kelas dasar yang tidak bisa dibuat objek langsung, hanya diwariskan.
// interface = seperti “janji” bahwa class yang menggunakannya harus punya method tertentu.
// extends = pewarisan class.
// implements = mengaktifkan interface yang disepakati.